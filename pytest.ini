[pytest]
testpaths = tests
asyncio_mode = auto
# Tests live at the repo root, not under backend/, because they cover
# scraper/ and workers/ as well as backend/app/ — all three are importable
# from here (PYTHONPATH=/srv in the scraper image, repo root locally).
addopts = -q --strict-markers
